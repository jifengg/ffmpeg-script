# 编写此文件时，每个参数用一行表示，前后不需要用引号，即使有空格也不需要引号，且含空格的行也算一个参数
# 如果有对齐参数的需求，可以在前面使用空格。
# 以#或//开头的为注释。目前只支持行注释。

-repeat
-fontsize
30
-boxw
450
-boxh
120
-alpha
0.75
# 顺时针旋转45度
-rotate
45
-text
仅限某某用途使用，它用无效